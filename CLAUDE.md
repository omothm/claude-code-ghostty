## Post-Change Checklist

After every change, verify the following:

1. **Tab targeting** -- Will notifications still focus the correct tab?
2. **Notification text** -- Does the notification text need updating to match?
3. **README.md** -- Does the documentation reflect the current behavior?
4. **Local deploy** -- Ask the user whether the change should be deployed to local config (`~/.claude`).
